<%- model_class = Service -%>
<div class="page-header">
  <h1><%= @service.name %></h1>
</div>


  <p><%= @service.description %></p>
  <img class="img-rounded "<%= image_tag @service.image_url.to_s %>

<div class="form-actions">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              root_path, :class => 'btn'  %>
</div>
